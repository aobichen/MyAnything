
@model IPagedList<Anything.ViewModels.HotelsViewModel>
@{
    ViewBag.Title = "Index";
    ViewBag.active = "Hotel";
}
<p>

</p>
<div class="row">
   
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-3">
                @Html.Partial("_NavSideBar")
            </div>
            <div class="col-md-9">
                <p>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="col-md-12">
    <div class="col-md-6">@Html.TextBox("SearchString", null, new { @class = "form-control" })</div>
        <div class="col-md-6">
        </div>
        <button type="submit" class="btn btn-default">
            search
        </button>

    </div>
                   
                    
}
                </p>
                @Html.Partial("_HotelTable",Model)
                @Html.PagedListPager(Model, Page => Url.Action("Index", new { Page }))
            </div>
        </div>
    </div>
   
 </div>




